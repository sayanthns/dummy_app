[project]
name = "dummy_app"
authors = [
    { name = "Enfono", email = "dev@enfono.com" }
]
description = "Dummy test app — verifies install/update/version flow"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.flit.module]
name = "dummy_app"

[tool.bench.frappe-dependencies]
frappe = ">=15.0.0,<16.0.0"
